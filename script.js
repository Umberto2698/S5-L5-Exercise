const header = document.getElementsByClassName("header")[0];
const button = document.getElementById("scroll-anchor");

header.addEventListener("scroll", (event) => {
  console.log(event);
});

// onscroll = (event) => {
//   console.log(event);
// };
